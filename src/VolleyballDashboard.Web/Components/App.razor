<!DOCTYPE html>
<html lang="en" class="h-full dark">
<head>
    <script>
        // Initialize theme BEFORE any content renders (prevents flash)
        (function() {
            const theme = localStorage.getItem('theme') || 'dark';
            document.documentElement.classList.toggle('dark', theme === 'dark');
            document.documentElement.classList.toggle('light', theme === 'light');
            if (theme === 'light') {
                document.documentElement.classList.remove('dark');
            }
        })();
        
        // Theme toggle function - attached to window for global access
        window.toggleTheme = function() {
            const isDark = document.documentElement.classList.contains('dark');
            if (isDark) {
                document.documentElement.classList.remove('dark');
                document.documentElement.classList.add('light');
                localStorage.setItem('theme', 'light');
            } else {
                document.documentElement.classList.remove('light');
                document.documentElement.classList.add('dark');
                localStorage.setItem('theme', 'dark');
            }
        };
    </script>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <title>Volleyball Dashboard</title>
    <link rel="icon" type="image/svg+xml" href="favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    fontFamily: {
                        'sans': ['Outfit', 'system-ui', 'sans-serif'],
                        'mono': ['JetBrains Mono', 'monospace'],
                    },
                    colors: {
                        volt: {
                            50: '#f0fdf4',
                            100: '#dcfce7',
                            200: '#bbf7d0',
                            300: '#86efac',
                            400: '#4ade80',
                            500: '#22c55e',
                            600: '#16a34a',
                            700: '#15803d',
                            800: '#166534',
                            900: '#14532d',
                        },
                        court: {
                            50: '#fef3e2',
                            100: '#fde6c4',
                            200: '#fbcd89',
                            300: '#f9ad4d',
                            400: '#f79623',
                            500: '#ee7d09',
                            600: '#cf5f05',
                            700: '#ac4408',
                            800: '#8c350e',
                            900: '#742d0f',
                        }
                    }
                }
            }
        }
    </script>
    <style>
        :root {
            --bg-primary: #0a0f1a;
            --bg-secondary: #111827;
            --bg-card: #1a2234;
            --accent-primary: #22c55e;
            --accent-secondary: #f79623;
        }
        .light {
            --bg-primary: #f8fafc;
            --bg-secondary: #ffffff;
            --bg-card: #ffffff;
        }
        body {
            background: var(--bg-primary);
        }
        .dark body {
            background: linear-gradient(135deg, #0a0f1a 0%, #111827 50%, #0f172a 100%);
        }
        .light body {
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 50%, #e2e8f0 100%);
        }
        .glass-card {
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
        }
        .dark .glass-card {
            background: rgba(26, 34, 52, 0.8);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }
        .light .glass-card {
            background: rgba(255, 255, 255, 0.9);
            border: 1px solid rgba(0, 0, 0, 0.05);
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }
        .live-pulse {
            animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
        }
        @@keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }
        .score-glow {
            text-shadow: 0 0 20px rgba(34, 197, 94, 0.5);
        }
        .gradient-border {
            position: relative;
        }
        .gradient-border::before {
            content: '';
            position: absolute;
            inset: 0;
            border-radius: inherit;
            padding: 1px;
            background: linear-gradient(135deg, #22c55e, #f79623);
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
        }
    </style>
    <HeadOutlet />
</head>
<body class="h-full font-sans antialiased">
    <Routes />
    <script src="_framework/blazor.web.js"></script>
    <script>
        // Get browser timezone offset in minutes (negative = ahead of UTC)
        window.getTimezoneOffset = function() {
            return new Date().getTimezoneOffset();
        };
    </script>
</body>
</html>
